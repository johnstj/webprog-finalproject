import React, { useState, useEffect } from 'react';
import './Rate.css';
import { useNavigate, useHistory } from "react-router-dom";

const Rate = () => {
    const [jwtToken, setJwtToken] = useState('');
    const history = useHistory();

    // useEffect(() => {
    //     const receivedToken = document.cookie
    // }, []);
}